import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    logoutButton: {
        alignItems: 'center',
        display: 'flex',
        flexDirection: 'row',
    }
})

export default styles;