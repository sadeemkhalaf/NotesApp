import { scaleByHeight } from "@/utils/appUtils";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textArea: {
        height: scaleByHeight(120),
    }
})

export default styles;